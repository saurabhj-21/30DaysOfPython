#Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Saurabh", [1,2]}
print(s)

#No.Sets are immutable, meaning that once a set is created, you cannot change its elements.